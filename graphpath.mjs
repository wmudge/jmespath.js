//import { Runtime, compile, search } from "./jmespath";

/*
export default Graph {

    Runtime.call(this);

    return {
        query: function(data, expression) {
            var ast = compile(expression);
        }
    }
}
*/

export const FOO = 42;
